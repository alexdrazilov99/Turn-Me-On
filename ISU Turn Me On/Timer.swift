//
//  Timer.swift
//  ISU Turn Me On
//
//  Created by Student on 2017-01-13.
//  Copyright © 2017 Alex. All rights reserved.
//

import Foundation

/*class MyTimer{
    var time = 0
    var timer = Timer()
    var thirtyTimer = Timer()
    var label: String
    
    init (label: String){
        self.label = label
    }
    
    func timers(){
        timer = Timer.scheduledTimer(timeInterval: 1, target: self, selector: #selector(action), userInfo: nil, repeats: true)
        thirtyTimer = Timer.scheduledTimer(timeInterval: 30, target: self, selector: #selector(thirtyAction), userInfo: nil, repeats: false)
    }
    
    @objc func action(){
        time += 1
        label = "\(time)"
    }
    
    @objc func thirtyAction(){
        timer.invalidate()
    }
}
*/
